{
  "name": "graphiql-with-explorer",
  "version": "2.0.0",
  "license": "MIT",
  "description": "A GraphiQL docker app",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --outfile=dist/index.js --format=esm --define:process.env.NODE_ENV=\\\"production\\\" --define:process.env.NODE_DEBUG=false --define:global=window --log-level=silent",
    "start": "npm run build && node server.js",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "assert": "^2.0.0",
    "ejs": "^3.0.1",
    "graphiql": "^0.17.5",
    "graphiql-explorer": "^0.5.1",
    "graphql": "14.5.8",
    "punycode": "^2.1.1",
    "react": "16.12.0",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.8.39",
    "express": "^4.17.1"
  }
}
