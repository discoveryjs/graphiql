{
  "name": "@discoveryjs/graphiql",
  "version": "1.2.0",
  "license": "MIT",
  "description": "A GraphiQL docker app",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "scripts": {
    "build:bundle": "esbuild --bundle --minify --outdir=dist --define:process.env.NODE_ENV=\\\"production\\\" --define:process.env.NODE_DEBUG=false --define:global=window --main-fields=module,browser,main",
    "build:js": "npm run build:bundle -- src/index.jsx src/create-discovery-instance.js --format=esm",
    "build:css": "npm run build:bundle -- src/index.css src/create-discovery-instance.css --log-level=error && node scripts/cleanup-css-hacks",
    "build": "npm run build:js && npm run build:css",
    "start": "npm run build && node scripts/server.js",
    "dev": "mkdir -p dev && node scripts/gen-html > dev/index.html && npm run build:bundle -- src/index.jsx src/create-discovery-instance.js src/index.css src/create-discovery-instance.css --outdir=dev --servedir=dev --format=esm",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@discoveryjs/discovery": "^1.0.0-beta.58",
    "@types/react": "^17.0.2",
    "assert": "^2.0.0",
    "css-tree": "^1.1.2",
    "dset": "^3.1.0",
    "ejs": "^3.0.1",
    "esbuild": "^0.9.0",
    "express": "^4.17.1",
    "graphiql": "1.4.0-rc.1",
    "graphiql-explorer": "^0.6.2",
    "graphql": "14.5.8",
    "punycode": "^2.1.1",
    "react": "16.12.0",
    "react-dom": "^17.0.1"
  }
}
